import { task } from "./Classes";
import "./styles/TaskMenu.scss";

interface TaskMenuProps {
  data: task;
}

function TaskMenu({ data }: TaskMenuProps) {
  return <section></section>;
}

export default TaskMenu;
